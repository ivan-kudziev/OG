<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="by.kipind.game.olympicgames"
    android:versionCode="15"
    android:versionName="6.007">

    <uses-sdk android:minSdkVersion="14" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Optional -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:name="by.kipind.game.olympicgames.GameSettings"

        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">


        <activity
            android:name="by.kipind.game.olympicgames.activitys.GameActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|mcc|mnc"
            android:label="@string/app_name"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="by.kipind.game.olympicgames.activitys.PlayerInfoActivity"
            android:screenOrientation="landscape"/>
        <activity
            android:name="by.kipind.game.leaderboard.LeaderBoardActivity"
            android:screenOrientation="landscape"/>


    </application>


</manifest>